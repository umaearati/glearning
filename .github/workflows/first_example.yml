name: My First Workflow

on:
  push:
    branches: ["main"]

jobs:
  first_job:
    runs-on: macos-latest

    steps:
      - name: welcome message
        run: echo "my first github action job"

      - name: list files
        run: ls

      - name: read files
        run: cat README.md
