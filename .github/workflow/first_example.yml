name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: mac
    steps:
      - name: welcome message
      - run: echo "my first github action job"

      - name: list files
      - run: ls

      - name: read files
      - run: cat README.md
